---
- version: v1alpha1
  group: charts.helm.k8s.io
  kind: Cockroachdb
  chart: helm-charts/cockroachdb
  # overrideValues:
  #   image.repository: $COCKROACH_IMAGE_REPOSITORY
  #   image.tag: $COCKROACH_IMAGE_TAG
  #   tls.init.image.repository: $COCKROACH_TLS_IMAGE_REPOSITORY
  #   tls.init.image.tag: $COCKROACH_TLS_IMAGE_TAG
